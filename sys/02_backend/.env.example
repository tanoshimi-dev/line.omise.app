# Server
PORT=8080

# Database (engine decided in dev-plan-02-database.md — placeholder shape below)
DATABASE_URL=postgres://user:password@localhost:5432/line_omise_app

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://line.omise.app

# --- The following are introduced by later dev-plans and listed here as a
# --- preview of what this file will grow to contain; leave unset until the
# --- corresponding step is implemented.

# LINE Login / Google OAuth — dev-plan-04-auth
# LINE_LOGIN_CHANNEL_ID=
# LINE_LOGIN_CHANNEL_SECRET=
# LINE_LOGIN_CALLBACK_URL=https://line.omise.app/auth/line/callback
# GOOGLE_OAUTH_CLIENT_ID=
# GOOGLE_OAUTH_CLIENT_SECRET=
# GOOGLE_OAUTH_CALLBACK_URL=https://line.omise.app/auth/google/callback
# ADMIN_EMAILS=
# SESSION_SECRET=
