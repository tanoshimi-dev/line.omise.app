# Step 10 — `/usecase/` 一覧・詳細 UI

**フェーズ:** Phase 1 — Frontend
**依存:** Step 07（Next.js 基盤）, Step 05（コンテンツ API）

---

## ゴール

導入店舗インタビュー（`/usecase/`）の一覧・詳細ページを実装する。
ログイン不要、SEO を意識した静的生成寄りのページとする。

---

## タスク

### 10.1 一覧ページ — `/usecase/`

- [ ] 事例カード一覧（店舗名・タイトル・サムネイル）
- [ ] `GET /api/usecases` から取得

### 10.2 詳細ページ — `/usecase/{client}/`

- [ ] インタビュー本文表示（Markdown レンダリング、`/learn/` の記事詳細と共通コンポーネント化を検討）
- [ ] 関連するミニアプリ（会員管理/サロン予約/スイーツショップ）へのリンクがあれば表示
  （Step 08 の外部リンク対応と合わせる）

### 10.3 SEO

- [ ] `generateMetadata` で title/description を設定
- [ ] `app/sitemap.ts` に `/usecase/` 配下を含める

---

## 成果物

- `src/app/usecase/` 配下のページ一式

## 完了条件

- 事例一覧・詳細がコンテンツ API から取得して表示される
- 未ログインで問題なく閲覧できる
